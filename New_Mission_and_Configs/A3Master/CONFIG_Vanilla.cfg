//
// server.cfg
//
// comments are written with "//" in front of them.

//Used for headless client
localclient[]={127.0.0.1};
headlessClients[]={127.0.0.1};
 
// GLOBAL SETTINGS
hostname = "WARIRON Altis GoldBuyer GoldMissions | Lock Vehicles | No Fog | Thermals ON ";		// The name of the server that shall be displayed in the public server list
//hostname = "SERVER";		// The name of the server that shall be displayed in the public server list

//hostname = "GameTrackerClaimServer ";		// The name of the server that shall be displayed in the public server list
///password = "www123";					// Password for joining, eg connecting to the server
passwordAdmin = "Ratzo6848";				// Password to become server admin. When you're in Arma MP and connected to the server, type '#login xyz'
//reportingIP = "armedass.master.gamespy.com";	// For ArmA1 publicly list your server on GameSpy. Leave empty for private servers
//reportingIP = "arma2pc.master.gamespy.com";	// For ArmA2 publicly list your server on GameSpy. Leave empty for private servers
//reportingIP = "arma2oapc.master.gamespy.com";	// For Arma2: Operation Arrowhead  //this option is deprecated since A2: OA version 1.63
//reportingIP = "arma3" //not used at all
logFile = "server_console.log";			// Tells ArmA-server where the logfile should go and what it should be called
 
 
// WELCOME MESSAGE ("message of the day")
// It can be several lines, separated by comma
// Empty messages "" will not be displayed at all but are only for increasing the interval
motd[] = {
	"", "",  
	//"Two empty lines above for increasing interval",
	"Welcome to WarIron. Please wait for the map to load .. it sometimes takes a while after restart",
	"", "",  
	//"New custom mission coming soon !",
	//"One more empty line below for increasing interval",
	""
};
motdInterval = 5;				// Time interval (in seconds) between each message
 
 
// JOINING RULES
checkfiles[] = {};				// Outdated.
maxPlayers = 80;				// Maximum amount of players. Civilians and watchers, beholder, bystanders and so on also count as player.
kickDuplicate = 1;				// Each ArmA version has its own ID. If kickDuplicate is set to 1, a player will be kicked when he joins a server where another player with the same ID is playing.
verifySignatures = 2;				// Verifies .pbos against .bisign files. Valid values 0 (disabled), 1 (prefer v2 sigs but accept v1 too) and 2 (only v2 sigs are allowed). 
//equalModRequired = 0;				// Outdated. If set to 1, player has to use exactly the same -mod= startup parameter as the server.
//requiredBuild = 12345				// Require clients joining to have at least build 12345 of game, preventing obsolete clients to connect
 
 
// VOTING
/*Not listing any command ( allowedVoteCmds[] = {}; ) means voting is disabled. 
Not specifing allowedVoteCmds at all means everything is allowed!
Example:

allowedVoteCmds[] = {
{"admin", true, true, 0.5},
{"missions", true, "true", "0.5"},
{"mission", true, true}, // will use global "voteThreshold"
{"kick", false, false, 0.75},
{"restart", false, true, -1}, // invalid threshold value. Will default to global "voteThreshold"
{"reassign", true, true, 0.5}
};
*/

allowedVoteCmds[] = {};              //voting disabled.
voteMissionPlayers = 300;				// Tells the server how many people must connect so that it displays the mission selection screen.
voteThreshold = 1;				// 33% or more players need to vote for something, for example an admin or a new map, to become effective
 
 
// INGAME SETTINGS
disableVoN = 0;					// If set to 1, Voice over Net will not be available
vonCodecQuality = 11;				// since 1.62.95417 supports range 1-20 //since 1.63.x will supports range 1-30 //8kHz is 0-10, 16kHz is 11-20, 32kHz is 21-30
persistent = 1;					// If 1, missions still run on even after the last player disconnected.
timeStampFormat = "none";			// Set the timestamp format used on each report line in server-side RPT file. Possible values are "none" (default),"short","full".
BattlEye = 1;					// Server to use BattlEye system

allowedLoadFileExtensions[] = {"hpp","sqs","sqf","fsm","cpp","paa","txt","xml","inc","ext","sqm","ods","fxy","lip","csv","kb","bik","bikb","html","htm","biedi"}; //only allow files with those extensions to be loaded via loadFile command (since Arma 3 build 1.19.124216)
allowedPreprocessFileExtensions[] = {"hpp","sqs","sqf","fsm","cpp","paa","txt","xml","inc","ext","sqm","ods","fxy","lip","csv","kb","bik","bikb","html","htm","biedi"}; //only allow files with those extensions to be loaded via preprocessFile/preprocessFileLineNumber commands (since Arma 3 build 1.19.124323)
allowedHTMLLoadExtensions[] = {"htm","html","xml","txt"}; //only allow files with those extensions to be loaded via HTMLLoad command (since Arma 3 build 1.27.126715)
 
 
// SCRIPTING ISSUES
onUserConnected = "";				//
onUserDisconnected = "";			//
doubleIdDetected = "";				//
//regularCheck = "{}";				//  Server checks files from time to time by hashing them and comparing the hash to the hash values of the clients. //deprecated
 
// SIGNATURE VERIFICATION
onUnsignedData = "kick (_this select 0)";	// unsigned data detected
onHackedData = "ban (_this select 0)";		// tampering of the signature detected
onDifferentData = "ban (_this select 0)";				// data with a valid signature, but different version than the one present on server detected
//onDifferentData = "";				// data with a valid signature, but different version than the one present on server detected
  // MISSIONS CYCLE (see below)
  class Missions
  {
         // class Mission1
         //{
         //     template = "altis%20royale.altis";
         //     difficulty="veteran"; // difficulty: recruit, regular, veteran or mercenary (see CfgDifficulties in the main game config)
         // };
  
            class Mission2
         {
		      
			  //template = "ArmA3_Wasteland_TA_WI.Altis";
			  
			  template = "WarIron.Altis";
			  
	  		  difficulty="regular"; // difficulty: recruit, regular, veteran or mercenary (see CfgDifficulties in the main game config)
          };
  };
  
  
  
  
  